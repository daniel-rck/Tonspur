# Per-app formatter exclusions. `.oxfmtrc.json` is owned by web-base and
# `update` overwrites it — put app-specific paths here instead (gitignore
# syntax; oxfmt reads this file next to .gitignore).

# Generated by `bun run fetch-links`; the generator writes unformatted output.
src/features/game/movies.ts
src/features/game/movie-links.ts
# Hand-packed, art-directed game skin.
src/features/game/game.css
